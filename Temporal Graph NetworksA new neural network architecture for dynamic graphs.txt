To solve the staleness problem, the embedding module computes the temporal embedding of a node by performing a graph aggregation over the spatiotemporal neighbours of that node. One such dynamic graph is Wikipedia, where users and pages are nodes, and an interaction represents a user editing a page. However, many interesting real-world graphs are dynamic and evolving in time, with prominent examples including social networks, financial transactions, and recommender systems. One example task is predicting future interactions by trying to answer the question: what is the probability of having an edge between nodes i and j at time t? The memory stores the states of all the nodes, acting as a compressed representation of the node’s past interactions. Temporal graph networks for deep learning on dynamic graphs (2020). Node insertions are considered as a special case of new edges when one of the endpoints is not presently in the graph.
