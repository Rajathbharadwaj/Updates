The face recognition model we are building will be able to detect the authorized owner’s face and will reject any other face. The collection of images is an important step and we will be collecting the images for only the people you want to grant access to your selective device. You can check out the link to the entire code as well as the code to loading the weights and final run model here. After this, we are free to create our custom model on top of the VGG-16 architecture by excluding the top layer. We can do this with the following code block.The ImageDataGenerator is used for data augmentation of images.We will be replicating and making copies of the transformations of theoriginal images. The model will provide a vocal response if the face is granted access or the access is denied. Let us look at what task each of these individual callbacks performs.We are compiling and fitting our model in the final step.
