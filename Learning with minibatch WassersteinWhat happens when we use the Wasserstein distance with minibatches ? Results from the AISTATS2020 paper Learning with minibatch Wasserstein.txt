However, we did not meet any problem while using the minibatch Wasserstein distance in practice.We illustrate the use of minibatch Wasserstein loss for generative modelling. In this story, I will describe the minibatch Wasserstein distance, where the minibatch paradigm consequences on the loss were left unjustified. Indeed, it does not compute the Wasserstein distance but the expectation of Wasserstein distance over minibatches sampled from input measures. Hence, the minibatch Wasserstein distance IS NOT a distance. The purpose of gradient flows is to model a distribution which at each iteration follows the gradient direction which minimizes the minibatch Wasserstein distance. However, even for 1M pixels, a batch size of 1000 is enough to keep a good diversity of colors.In this article, we describe the minibatch Wasserstein distance. More extensive results using minibatch Sinkhorn Divergence on the CIFAR 10 datasets are available in .In this section, we describe the use of a minibatch Wasserstein gradient flow.
