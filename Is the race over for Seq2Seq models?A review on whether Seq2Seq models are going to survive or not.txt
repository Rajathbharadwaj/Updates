Basically the self-attention layer, while processing a single word in the sequence, enables the model to look at all the other words. One particular example is found in the paper Mogrifier LSTM .It is no brainer that, it has been proven constantly that Transformer models outperform sequential models almost always. During the model training, you also have an n_t elements long text sequence, drawing words from a V_t size vocabulary (French). Imagine the following sentence,The dog ran across the road to get its ballNow visualize a model going from one word to another sequentially. Note that I will not be discussing all the intricacies of the Transformer model, but just enough to understand how it differs from the Seq2Seq model. You can quickly have Seq2Seq model up and running compared to a Transformer, as Seq2Seq models are much simpler and easier to understand. It’s a very innovative concept and is addressing two major weaknesses in RNNs:Here to understand the Transformer model better, we will assume a translation task from English to French.
