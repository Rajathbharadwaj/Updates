, to extract the features for the main image content layer we should select block1_conv2 And for the style layers we need to choose block1_conv2, block2_conv2, block3_conv3, block4_conv3, block5_conv3. However, you can try different combinations to get more accurate Style Transfer.Once we have finalized on the CNN model, we can now define a main loss function. Try tweaking the hyper-parameters, optimizers and also other CNN architectures to get new and different results. The output size will be (512,512,3).
