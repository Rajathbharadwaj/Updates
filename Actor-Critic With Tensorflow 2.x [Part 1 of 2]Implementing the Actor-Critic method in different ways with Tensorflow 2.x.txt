So our update rule for actor-critic will look like the following.In Actor-Critic Methods, we have two neural networks namely Actor and a critic. The actor is used for action selection and Critic is used to calculate state value. You can find very good resources for theory purposes in the reference section.In this implementation, we will be updating our neural networks on each timestamp. If you look at the update equation then you can notice that state value is being used as a baseline. And the common baseline use for these methods is the state value function. Stay tuned for upcoming articles where we will be implementing A2C with and without multiple workers.So, this concludes this article. This implementation differs from A2C where we update our network after every n timestamp.
