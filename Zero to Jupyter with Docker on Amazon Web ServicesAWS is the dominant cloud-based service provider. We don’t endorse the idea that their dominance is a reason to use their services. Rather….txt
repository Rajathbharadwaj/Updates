Key pairs created in one region will not be available in another.We have created a key pair between AWS and our local system. We display a public key that was created on a remote ubuntu system, as can be seen at the end of the key (ubuntu@ip-172–31–43–19). We will create an ssh key pair locally and import the public component of the key pair into AWS. For us, this is a secure connection between our local system and a sandbox system we will configure on an EC2 instance. Check the box acknowledging access to that key pair and launch the instance.You should see a notification that the instance is now running. id_rsa.From the EC2 Dashboard access the Security Group pane using either link.From the Security Group pane, click “Create Security Group”. To be clear, we are uploading the public component of a key pair we have created on our system e.g.
