After that, you may use that into JSON and do whatever you want to do.In this article, I have introduced the library attrs and some of the basic usage. Firstly, the @attrs is the annotation that tells this class is going to be declared with the attrs library. Yes, with attrs you can do that easily.As shown, we give both the attributes default values. It is also very easy to implemented using attrs.In the above example, we have declared a “Student” class with an attribute “student_id”. Suppose that we want to validate the student id that it must be 6 characters long.Note that we need to define a function to do this. What if there is such an attribute that we don’t want to initialise it during instantiation but probably assign value to it later? Usually, we don’t want to pass an in as an argument, this is one of the famous traps of Python that may cause a lot of unexpected problems.
