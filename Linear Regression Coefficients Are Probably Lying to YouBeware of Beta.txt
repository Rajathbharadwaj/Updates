Interpreting linear regression coefficients is common to do, because it’s so easy. For instance, we may be inclined to say that ‘one point on the GRE will increase chances by x%’, under the assumption that all other factors remain fixed. The result should be the y-variable.Let’s put linear regression in context. We would have gotten different results if we expanded our dataset, and an expansion of data usually means getting closer to the truth. One can imagine how damaging this could be in a business context.Largely, most of the problems described can be traced to multicollinearity, which is inevitable in every real-world dataset. Similarly, ‘losing one point on the GRE’ will most likely see a decrease larger than x%’. It multiplies each of the inputs by a value and adds them up — as an additional degree of freedom, an ‘intercept’ can be added.
