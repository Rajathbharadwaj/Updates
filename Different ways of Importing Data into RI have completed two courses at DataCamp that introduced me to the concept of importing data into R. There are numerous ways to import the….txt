We can also export the data into various formats.Let’s install the package and load the library.SASThe drawback of this package is it cannot import .sas7bdat. The typical structure of excel file contains different sheets with tabular data.We need to explore the files and then import some data from it. They are mostly used to format the JSON data.This package is used to read SAS, STATA, SPSS data files. After a successful installation, we will be using fromJSON function to get the data from the URL.Another interesting function from the package is prettify and minify. I have completed two courses at DataCamp that introduced me to the concept of importing data into R. There are numerous ways to import the data. I tend to use this always.read_r supports seven file formats with seven functions:readr package work with Tibbles. It is less consistent in naming and use but it supports many foreign data formats like Systat, Weka, etc.
