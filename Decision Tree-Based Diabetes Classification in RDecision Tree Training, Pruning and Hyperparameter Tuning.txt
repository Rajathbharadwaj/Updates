(e) Best model evaluation on test dataAfter identifying the best performing model, the next step is to see how accurate the model is. Both contain 9 variables.The next step is the model training and evaluation of model performanceFor decision tree training, we will use the rpart( ) function from the rpart library. This could be done through the following steps:(d) Identifying the best modelThe next step is to retrieve the best performing model (maximum accuracy) and printing its hyperparameters using model$control. For predicting the test data class we need to supply the model object, test dataset and the type = “class” inside the predict( ) function. (f) Best model plotNow it is time to plot the best model.Even the above plot is for best performing model, still, it looks a little bit fragile. Next, based on indexing we split out the train and test data.The train data includes 318 observations and test data included 74 observations. (a) Plotting the error vs complexity parameterThe decision tree has one parameter called complexity parameter (cp) which controls the size of the decision tree.
