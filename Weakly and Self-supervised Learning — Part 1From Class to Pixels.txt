That would be essentially supervised learning: Training of bounding boxes to predict bounding boxes and training with pixel labels to predict pixel labels. Of course, you can also use hints like image labels, bounding boxes, and image captions as weakly supervised labels. Of course, you could also abstract from pixel-level labels to bounding boxes or from bounding boxes to image labels. Here, you see that if we have image labels, of course, we can classify image labels and train. So with this, we can produce class activation maps and use that as a label for localization.We can also go from bounding boxes to segmentation. So, using a heuristic method can also help to improve the labels from bounding boxes to pixel-wise labels. You can see that in .Now, the difference between weakly supervised learning and strong supervision, you can see in this graph.
