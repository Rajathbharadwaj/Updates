If the training error is small, plot learning curves and try to deduce from them whether the problem is estimation error or approximation error.4. We will use the train set when training our model, and we will use the independent test set to validate our model in order to get the empirical error. If the training error is excessively large consider enlarging the hypothesis class, completely change it, or change the feature representation of the data.3. A large hypothesis class often increases the estimation error because it makes it more difficult to find a good predictor. It is necessary that we have a sufficient amount training samples in order to improve estimation error. This weight is then updated where if the hypothesis is correct, it will get a lower probability weight and the contrary with the examples where the hypothesis is incorrect. Imagine you have built a face recognition model, and are now using a validation set to tune the parameters for your experimentation on a test set.
