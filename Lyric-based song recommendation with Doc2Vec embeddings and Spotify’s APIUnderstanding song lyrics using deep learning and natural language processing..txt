If available, the user’s top artists are suggested to them for inspiration.Once an initial song is selected, similar songs are found by measuring the cosine similarity between embeddings. In general, we should expect to see songs by similar artists closer together than songs by artists from different genres.Flask is a python library for building web applications. This can be done with a Distributed Bag of Words Paragraph Vector (PV-DBOW) model, which works in the following way.In our case, a paragraph is actually a song. These weights include those that make up the paragraph vectors.If we did have the original lyrics, we could use Distributed Memory Paragraph Vectors (PV-DM). Therefore, we can plot points in low dimensions to get an idea of the relationships in the high dimensional space.When applied to the musiXmatch dataset, it looks like this. This model could potentially improve our results by taking into account the order in which words appear. We try to predict that word using the paragraph matrix as input and make updates to the model weights.
