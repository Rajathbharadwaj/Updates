One way to achieve this is to combine data from many different data organizations or data owners. Load data from the different data owners into a single `DMatrix` at the server.4. That is, even though the training runs in the cloud, each client’s data remains hidden from the cloud provider and other clients. The possibilities are vast and promising.As a step in this direction, we’re happy to introduce Secure XGBoost, a library that enables collaborative XGBoost training and inference on encrypted data. TL;DR: In the RISE Lab at UC Berkeley, we’ve been building Secure XGBoost, a library that enables collaborative XGBoost training and inference on encrypted data. As a result, the memory access patterns reveal no information about the underlying data to the attacker.However, the extra security comes at the cost of performance. Any data or software placed within the enclave is isolated from the rest of the system.
