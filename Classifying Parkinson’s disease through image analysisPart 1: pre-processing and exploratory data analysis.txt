The medial axis also returns a distance map, but skeletonize is cleaner as it does some pruning.Plotting the mean and standard deviation we see some correlation between the drawings. The advantage of using Random Forest is we will also be able to see which features provide the strongest impact to the model. In addition, we can calculate the number of intersection points; a perfect curve would have no intersections and only two end-points. In part 2 of this series we will perform classification using these metrics and also compare to a more powerful, but black-box, neural network. Connectivity 1 has direct neighbors, connected 2 includes diagonals.Here is the result:As you can see we now have a single pixel with value 3 at the intersection location. We can now simply sum these locations to quantify the number of intersection points. We first read in and invert the image so the drawings are white on black background, as well as resize if necessary.
