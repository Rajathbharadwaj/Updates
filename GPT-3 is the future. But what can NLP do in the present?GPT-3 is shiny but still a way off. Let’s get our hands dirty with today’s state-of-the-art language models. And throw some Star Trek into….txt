Like GPT-2 (which this model is based on), examples took about 10 minutes each:In short, neural search is a new approach to retrieving information. It took about a minute to generate each result:In short, neural search is a new approach to retrieving information. I have a feeling these AI’s may be smarter (and more emotional) than we think.After restarting:In short, neural search is a new approach to retrieving information. Here’s what it spat out after about 10 minutes:In short, neural search is a new approach to retrieving information. One of the most important goals of neural search is to let them search even more easily so they can make better decisions. A new way forward.FARRIS: The key is the neural patterns themselves, not the computer.SISKO: But a computer can do this.FARRIS: It’s a breakthrough in computational neural net theory. But even if the information is readily searchable, there is still the problem of how to combine multiple search terms in the database.
