So, this is then instance segmentation which will be in the last video about these topics.So, let’s go ahead and talk a bit about ideas towards image segmentation. Now in image segmentation, we want to find exactly which pixels belong to that specific class. We want to look into image processing and in particular into segmentation and object detection.So let’s see what I have here for you. So in semantic segmentation, you then try to find the class of every pixel in the image. Then we want to go into several techniques that allow you to do good image segmentation. Actually, in the next video, we will then talk about how to actually integrate the encoder and the decoder to get good segmentation masks. Now lastly when we have mastered those two ideas, then we also want to talk about the problem of instance segmentation.
