If you want to get multiple artists you could create a loop like so:The class returns a Pandas DataFrame which looks like this:Great, now that we have our data. I think the visualization below quite clearly brings this to life.The lyrics of different genres aren’t entirely mutually exclusive — unless you compare the extremes. I find you can get away with scraping c.250 songs before this happens.MusixmatchScraper() is coded in a way that allows you to scrape the content for one artist. We can then use various techniques that can show us which vectors/lyrics are similar or different.Vector representations of words were a significant step-change for natural language processing. Death metal, heavy metal, and rock differ slightly from one another but vastly different to pop and soul. The sentences that are most similar have more similar vectors (cosine similarity of 0.41 / 0.51). These word embeddings represent a word’s relationship with a neighboring word and therefore its context.
