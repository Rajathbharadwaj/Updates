This technique is called higher order function.Here we pass the function (double or triple) as the input for function operate. Various different names can be bound to the same function object.For example:Here we assign the function to a variable. Below is the output of the test_decorator:Functions in Python can also take other function as input, then return other function. In Python, everything is object, which means every entity has some metadata (called attributes) and associated functionality (called methods). We will notice that after the variable is assigned, it became a function. These attributes and methods are accessed via the dot syntax. Names that we define are simply identifiers bound to these objects.
