For a given point p, the silhouette coefficient s(p) is used to assess whether this point belongs to the “right” cluster. The homogeneity of a cluster can then be defined as the average of the distances of each of the points contained in this cluster to the centroid. The choice of the distance measure is therefore important.The shape of a cluster is an important element that we initially describe as:Often, we search for clusters tighten on themselves. ).Outside the geodata scope, inferring data properties is useful for:In the examples above, the subjects (image, text, pixels) are represented as 2D/3D/nD points and are then grouped into clusters. In this way, a tightened cluster will have a lower heterogeneity than a cluster of scattered points. Explications and Illustration over 3D point cloud data.Clustering algorithms allow data to be partitioned into subgroups, or clusters, in an unsupervised manner. First, we can compute the centroid of a cluster (the barycentre of the points of this cluster) pretty easily.
