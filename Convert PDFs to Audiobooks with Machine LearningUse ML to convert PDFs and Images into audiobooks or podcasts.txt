Meanwhile, body text is the most common text in a document. For each chunk of text, he created a set of features to describe it: how many characters were in the chunk of text? ).One tricky bit here, though, is that the body text font size falls in a range (not one fixed value). Here’s what it looks like:First, we’ll extract the text from the document using OCR. For example: the title of a paper is probably written in the largest text size. It groups text into chunks (pages, blocks, paragraphs, words, and characters) and returns its location on the page. Meanwhile, that long span of points in the middle, that’s the body text.
