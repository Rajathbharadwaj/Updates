We can configure the chart type using the type key inside the hash of the data array. Now save the following code inside an HTML file, like test.html.By loading this file on your browser, you can see a line graph like the one below. By using data visualization, such as charts and maps, we can represent data graphically. When it’s easier to process data, we can improve our judgment and decision making, and it’s possible when we have an effective way to display data. In recent years data visualization has become one of the primary tools for decision making in healthcare, not-for-profits, and many other sectors. The hash also takes latitude, longitude, and hoverinfo data. The config hash is used to add extra configurations to the chart.
