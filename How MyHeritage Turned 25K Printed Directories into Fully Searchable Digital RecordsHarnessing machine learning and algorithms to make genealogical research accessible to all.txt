The statistical data collected allowed us to reject advertisement blocks and outline valuable regions.After the computation of the learning set, we processed the whole book. It’s important to find the correct learning set since the statistical data collected on this set will affect further processing. The primary line is a record which starts with a surname, the secondary line is a record which should infer the previous surname, but represents another person. We worked together with a third-party vendor to find and tag the front cover and back cover of each book to segment the data. If the learning set consists of advertisement pages only, whole book processing will fail. In specific books or columns secondary lines and (or) line breaks could be absent. Specifically, it analyzes the OCR output: font sizes, block sizes, and average line numbers in the structure of blocks and columns on the learning set pages.
