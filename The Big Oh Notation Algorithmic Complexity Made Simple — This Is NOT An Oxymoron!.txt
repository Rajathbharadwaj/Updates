This makes the time complexity of binary search O(logN).The number of steps barely increase, as the input grows (i.e. This is why it is called constant time.An algorithm that is O(N) will take as many steps as there are elements of data. An algorithm’s performance depends on the number of steps it takes. Let’s quickly cover them:An algorithm of this complexity class is doing log(N) work N times and therefore its performance is slightly worse than O(N). it takes just one additional step each time the data doubles):By now we have learnt the four most important Big-O taxonomies. → At exactly 50 elements the two algorithms take the same number of steps. We will talk about it another time :)An algorithm’s Big-O notation is determined by how it responds to different sizes of a given dataset.
