In this story, I will show how a dataloader for Bengali.AI Kaggle competition can be created using fastai in just 12 lines of clean code. Nevertheless, the train and validation dataloaders — dls.train and dls.valid — can be called in a loop to extract batches of data as exemplified in the image below. Having a framework that by default provides efficient code allows the deep learning practitioner to allocate more time to experiments, increasing the likelihood of a better end result. Having concise code also reduces the chance of small bugs that may silently affect the performance of the models. The fastai high-level API comprises two components: DataBlock and Learner. Other libraries have tended to force a choice between conciseness and speed of development, or flexibility and expressivity, but not both.
