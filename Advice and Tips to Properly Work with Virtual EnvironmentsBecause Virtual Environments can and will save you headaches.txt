Do NOT put your Python script inside the virtual environmentThe easiest way to delete your virtual environment is to simply manually delete it’s folder. Create virtual environment with a specific Python versionIf you want your script to be run with a specific Python version, it can be done quite easily. Leaving everything separate avoids problems.The best thing to do is to leave those files outside the virtual environment, like below:4. always using the correct versions for your packages.All codes coming up can be executed directly from the Anaconda prompt or the command line.It’s super easy. Eventually, we realized that the problem was caused by the fact we had different packages versions installed. Then, I shared my code with my teammate so he could start working on it too. In early 2020, I was working on a school project using Keras/Tensorflow.
