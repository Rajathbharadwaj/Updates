It can help to prevent overfitting.num_estimators sets the number of boosting rounds, which equals setting the number of boosted trees to use. XGBoost stands for eXtreme Gradient Boosting and it’s an open-source implementation of the gradient boosted trees algorithm. If set to 0, then there is no limit for trees’ depth.subsample is the size of the sample ratio to be used when training the predictors. You now want to create a classification model, to predict who will survive, based on this data. Beyond that, its implementation was specifically engineered for optimal performance and speed.Historically, XGBoost has performed quite well for structured, tabular data. The greater these numbers, the more conservative (less prone to overfitting but might miss relevant information) the model becomes. Recommended values lie between 0–1000 for both.max_depth sets the maximum depth of the decision trees.
