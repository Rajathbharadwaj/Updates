The code below if fairly well commented, so let us just quickly get past training and validation to the interesting part.Again, prediction based on input samples is pretty good. The state the neurons evolve to in one time instance is used an additional input to the neurons in the next time instance. As you see, the predictions match the expected output to a reasonable degree of accuracy.But predictions based on input samples are easy. Let us go ahead and import the usuals.Let us use the sum of sinusoids as the input time series. We all understand deep neural network, which are simply a set of neurons per layer interconnected sequentially to another set of neurons in the next layer and so on. We can visually see that the generated output based on its own past predictions matches the input pattern. So far so good!Let us build a CNN now, but only use 16 inputs at a time to predict the next sample.
