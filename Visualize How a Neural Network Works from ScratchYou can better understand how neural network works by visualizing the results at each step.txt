We can use the cross-entropy:The function can coded as below:Since there are 7 parameters, visualizing the cost function is not easy. Since standard packages don’t give all details of how the parameters are found, we will code a neural network from scratch. We can visualize this dataset.For those of you who already know how a neural network works, you should be able to find a simple structure by seeing this graph. (You should get 1.0, otherwise, you may have to run the code again because of local minimums).So great, how can we visualize the results of the algorithm? And to obtain the average value, we have to divide by the number of observations.Now, let continue for the parameters of the output layer. In the following code, we specify the number of hidden layers and the number of neurons with the argument hidden_layer_sizes.Then we can calculate the score. We can show the partial derivative with respect to b1 again.The matrix d_b1_v is the partial derivative for all observations.
