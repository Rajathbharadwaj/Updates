On the other hand, knowing whether the first flip is Heads certainly gives us information about whether both flips are Tails. The coin flip variable equals 0 with probability 0.5 and 1 with probability 0.5. A very simple random variable equals 1 if a coin flip turns up heads and 0 if the flip turns up tails. The range(10) variable has a distribution that assigns probability 0.1 to each of the numbers from 0 to 9.Often we’ll want to model distributions across a continuum of outcomes. A more complicated one might measure the number of heads observed when flipping a coin 10 times or a value picked from range(10) where each number is equally likely. (If the first flip is Heads, then definitely it’s not the case that both flips are Tails.) The probability of a farmer given the description will be:So, if we guess the candidate is likely a librarian.
