We can use the as expression only converting between primitive types.as is most commonly used to turn primitive types into other primitive types. We create a function called unique_pathswith two parameters mand n, which are i32 types.In function, we must declare the type of each parameter. Functions can return values and we declare their type for the return value using an arrow ->. We use a keyword struct and set fields with its type within the curly bracket. i32 is one of the primitive data types and a 32-bit fixed-sized signed integer types. Every variable, item, and value in a Rust program has a type.The type of value defines the interpretation of the memory holding it. Rust's compiler is able to infer the types of the parameters and most variables.Functions use the fn keyword to start.
