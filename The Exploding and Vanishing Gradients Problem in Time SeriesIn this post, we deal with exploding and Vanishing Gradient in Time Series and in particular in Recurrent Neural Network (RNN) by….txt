It is known that in the backpropagation training scheme, there are a forward pass and a backward pass through the entire sequence to compute the loss and the gradient. This problem is called the vanishing gradient. The challenge during the training is in the ratio of the hidden state:Two common problems that occur during the backpropagation of time-series data are the vanishing and exploding gradients. The second part is the backward pass, where the various derivatives are calculated.The training of RNN is not trivial, as we backpropagate gradients through layers and also through time. It is a very simple and very effective condition.By applying the gradient clipping, we do not change the gradient direction, but only its magnitude. By taking a window, we also improve the training performance from the training duration aspect- where we shortcut it.This window is called a “chunk”. “Why gradient clipping accelerates training: A theoretical justification for adaptivity.” arXiv preprint arXiv:1905.11881 (2019).
