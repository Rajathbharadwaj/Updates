But, the first set of weights are fed into a hidden layer, which then has another set of weights leading to the predicted output and the loss. As the model travels to find an optimal set of weights, it needs to update its the weights by some factor. We would have just initialized some weights on the pathways, but the model currently does not know what is right from wrong; and thus, the weights are incorrect. The loss optimization should attempt to find a set of weights, W, that will minimize the calculated loss. Consider the neural network shown above, how does a small change in the first set of weights affect the final loss? Therefore, since our current problem is a binary classification, an appropriate loss calculation would be a cross-entropy loss. These inputs are fed into the model, and the output predicts that the student has a 5% chance of passing the course.
