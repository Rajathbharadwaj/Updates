We’re also converting it into a set called 'entities_set'.Next, let’s count the frequency of the entities and append it to the list of tuples entities_counts. Let’s also set a random seed for reproducibility.Let’s read the data into a dataframe. This will save us a lot of loading time later.Now let’s load the contents stopwords file into the variable stopswords. In a previous post, we set out to explore the dataset provided by the Trump Twitter Archive. Exploratory data analysis uses a lot of techniques and we’ve only explored a few on this post. If you want to follow along, you can download the cleaned dataset here along with the file for stop words¹. If it is, the entity is then appended to 'preproc_pipe' and subsequently returned to its caller.
