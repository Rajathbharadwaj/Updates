In its simplest form it looks like this:This is nice as it shortens and simplifies code, but I personally rarely get a chance to use it. So, use idioms to make your code more readable, concise and effective and not just because it’s idiomatic. It also improves performance, especially if you would use x is None instead of x == None inside loop. One reasonable solution for this case could be to use None as default (when it's None do X, when it's not do Y). There are cases however, where you should always use is operator instead. By doing this we avoid any possibly acceptable values, because we're actually not checking value at all - we are checking identity. You probably already know about with statement and it's usage, but you might not know about contextlib used above.
