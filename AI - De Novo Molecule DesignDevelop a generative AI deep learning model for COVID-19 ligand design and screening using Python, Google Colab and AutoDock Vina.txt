The model achieved 99.66% accuracy on the test dataset.The baseline model is now broken up into constituent parts that will be used to build a generative model. The neural network model layers are defined exactly as before, except with a new input batch_shape (which is required for stateful LSTM model). and stop (E) characters to our character set.The neural network will be a sequence-to-sequence model that learns how to reproduce molecules one character at a time. Deep learning AI can be used to explore the synthetic molecule solution space and generate novel molecules for target binding.Computational design exists in the realm of virtual space. Then a generative model is built from the trained neural network to design new synthetic molecules. Make note of the molecule center coordinates and your search box dimensions.Our AI generative model output the sampled molecules in the SMILES format. For this model, a new input layer is defined that matches the latent space output by the encoder.
