After getting results from those sub dataframes, it applies a filter to those results (in our case, it’s “RegionAverageLife”>75). Finally, you’ll get a different dataframe whose length is the number of unique values of the groupby keys (“Region” in our case). It broadcasts the results from sub dataframes to the original full dataframe. Aggregation, Transform, and Filter bring goupby to another level.First, let’s take a glance at how groupby function works. The split step breaks up the dataframe into subset dataframes based on the specified keys. Thus transform will always return a series with the same length to the original full dataframe. You could view it as left merge the results to original full dataframe.
