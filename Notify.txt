This package provides a Jupyter notebook cell magic %%notify that notifies the user upon completion of a potentially long-running cell via a browser push notification.  A Jupyter Notebook magic for browser notifications of cell completion Use Git or checkout with SVN using the web URL. This magic allows you to navigate away to other work (or even another Mac desktop entirely) and still get a notification when your cell completes. A Jupyter Notebook magic for browser notifications of cell completion Use cases include long-running machine learning models, grid searches, or Spark computations. However, you'll need to have the jupyter notebook running on https. Work fast with our official CLI.
