The following example uses whitespace+newline engine.PyThaiNLP also comes with a tokenizer function for word segmentation. Let’s have a look at the following example.By default, it uses crfcut engine to do the sentence tokenization. This module provides a convenient function to parse a datetime object into your desired format just like how you would do it via datetime.strftime(). I have added a capital O in between %d.You can display a datetime object or a string (HH:MM:SS) entirely in Thai via time_to_thaiword() function. Import it as follow:Then, you can easily call it by passing a string to it. The best way is to loop over the list and call it one by one. We tried out a few example especially on the text tokenization, part-of-speech taggging and named-entity recognition.Thanks for reading this piece.
