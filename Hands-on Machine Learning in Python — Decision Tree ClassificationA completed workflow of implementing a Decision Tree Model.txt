Let the algorithm tells us.So, let’s build our feature data frame and label series.Now we can train our model using the features. Pandas data frame can also do that for us very easily.The describe() function will automatically select numeric features and calculates their statistics for us. The gender_submission.csv is not relevant to the machine learning model, so please ignore it.The instructions of the dataset, including the variables, train/test sets, etc. For example, sometimes the performance of the model is not quite well after we have done the model evaluation, and we still have other ideas to try. Usually, it is not quite easy to be “seen” how the model looks like for most of the machine learning algorithms. Simply call info() function of the data frame as follows.It is also very important to check the statistics of the features. It may be necessary to conduct workshops with the data owners and other stakeholders.Trust me, clarifying a dataset is not an easy job.
