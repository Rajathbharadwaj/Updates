This time, the example’s method iterates through the result set returned by the procedure and constructs an ordered collection of products, List<Product> object. This object can then be used to execute this statement multiple times efficiently.In this example, the average product weight query has been moved into a stored procedure. This example demonstrates one of the simplest methods for returning data from SQL Server.Next, we will execute almost the same static SQL statement as in Example 1. We can now call that column by name when retrieving the value.The stored procedure patterns found in Examples 3 and 4 are both commonly used. There are several reasons stored procedures are preferred:For brevity, I will use an existing and well-known Microsoft SQL Server database, AdventureWorks. The stored procedure returns a result set containing several columns of product information. To call the stored procedure, we use the java.sql.CallableStatement class.
