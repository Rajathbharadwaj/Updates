This function has one parameter, num, and it returns that number squared using the ** operator.Let’s now write this function as a lambda expression:And that’s it! However, we are only going to be using this function once and thus it seems unnecessary to create an entire function with the def keyword for that one task. They are useful when we need to create a function that will only need to be used once (a throw-away function) and can be written in one line. For example:Let’s make a lambda function that has two parameters instead of just one. We can do so by using the def keyword:We used the def keyword to define this function. Lambda functions can have any number of parameters but can only have one expression. Well, that’s where lambda expressions come in.Lambda expressions are used to create anonymous functions, or functions without a name.
