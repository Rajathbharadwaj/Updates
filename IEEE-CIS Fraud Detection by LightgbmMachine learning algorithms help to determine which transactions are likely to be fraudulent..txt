The code is written below for removing these unimportant features.Categorical data are the data that need only a limited number of possible values. Here I am using Pandas get_dummy function to convert categorical variables into dummy/indicator variables.There are several methods to deal with missing values like imputation, erase them. So we can remove them without losing much data.From all features, I have removed columns with more than 30% missing values. A snippet of code is written below shows that only 3.5% are fraudulent.They are several methods to deal with an unbalanced dataset as oversampling and undersampling. As you can see memory usage is reduced by almost 50%.Most of the transactions are not fraud so in this case, we have unbalanced data. Machine learning algorithms do not work properly with an unbalanced dataset. With this dataset, we apply the Lightgbm algorithm on a challenging large e-scale dataset.
