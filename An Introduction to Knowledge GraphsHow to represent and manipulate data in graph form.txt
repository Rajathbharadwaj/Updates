For example, let’s say we have two latent features (H_e = 2): being a good actor, and being a prestigious award. For example, Freebase had around 40M entities and 35k relations, giving 10¹⁹ possible triples.However, only a small subset of these triples will actually be feasible, due to certain constraints. For example we can combine RESCAL and PRA:This way PRA can model the observable graph patterns and RESCAL can model the “residual errors” that cannot be modelled by PRA. The paper makes the example of the weights learned to predict triples (p, college, c), so predicting which college a person has attended. However, triples are incomplete: that is, some of the nodes in the graph are connected, but there are also pairs of nodes that should be connected but aren’t. There could also be duplicates of entities and relationships.Let’s call e_i the subject node (eg: Spock), e_j the object node (Star Trek), and r_k the relationship type (characterIn). This fact is formed by the two nodes Spock and Star Trek and the relation characterIn forming the SPO triple (Spock, characterIn, Star Trek).So, existing edges indicate known facts.
