These are candidates for dropping, while those with fewer missing values might benefit from imputation.Secondly, we can often times see patterns of missing rows stretching across many features. You can download it here or use any arbitrary data you want to follow along.Determining data quality before starting to work on a dataset is crucial. After data cleaning, the memory usage drops to only 58.4 MB, a reduction of almost 80%! In our example we can see that if we drop rows with more than 30 missing values, we only lose a few entries. In this example we can see that for the column “play_type” roughly 75% of all entries are made up of the three most frequent values. The function klib.cat_plot() allows to display the top and/or bottom values regarding their frequency in each column. Additionally, 21 columns are of type “object”, which is a rather inefficient way to store data.
