It’s either a brute force search (for very small datasets), or one of the popular data structures for nearest neighbor lookups, k-d tree or ball tree. Results are averages of 5 runs.On average, training is almost 300 times faster, while prediction is about 7.5 times faster on average. As the dimensionality (number of features) of the data increases, the time needed to find nearest neighbors rises very quickly. Also note that for the MNIST dataset, which has size realistic for modern datasets, we get 17 times speedup, which is huge. The code is quite simple, since we just mimic the Scikit-learn API.There are a few interesting elements here:I’ve chosen a few popular datasets available in Scikit-learn for comparison. k Nearest Neighbors (kNN) is a simple ML algorithm for classification and regression. faiss offers CPU and GPU support, many different metrics, makes use of multiple cores, GPUs and machines and much more.
