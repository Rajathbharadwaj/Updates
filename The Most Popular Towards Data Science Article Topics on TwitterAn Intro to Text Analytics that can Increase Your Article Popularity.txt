This removes all the words that are part of both the tweets data set and the stop_words data set (essentially this removes the stop words from your data). These are typically the most common words of a language, but there may be additional words you’d like to remove for a specific domain. In our task we might consider the word “data” to be trivial since almost every article will mention data.Tokenization is the process of splitting of text into sections. If you’ve written data science articles or are trying to get started, finding the most popular topics is a big help in getting your articles read. Stop words are words that are not important to understanding the meaning of a passage. We can use the gsub() function to replace occurrences of links and handles with something else, in our case we remove them by replacing them with empty strings. Install these withMake sure you load each of these packages in your script with the library() function.To get tweets we first need to generate a token.
