And the dropout layers didn’t stop the third model from overfitting anyway.First I need to create the final model, training model_1’s architecture on the full dataset. The relevant balance (total_bal_il) and credit limit (total_il_high_credit_limit) metrics appear to already be in the data, so perhaps this utilization metric doesn’t contain any new information. I mentioned a couple of ideas along the way above for how I could improve the model in the future, but what’s the first thing you would tweak? Apparently the additional records in the first dataset did more to aid in training than the additional metrics in the subsequent sets. I’ll also merge the “credit policy” columns with their matching status.Now to create the output variable. Why would that be?Peeking back up to the data dictionary, il_util is the “ratio of total current balance to high credit/credit limit on all install acct”. I used ReLU activation in those hidden layers, and adam optimization and a loss metric of mean squared error in the model as a whole.
