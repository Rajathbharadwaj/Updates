), to do this, let’s go to the TEST & USE tab.Before we can try our model, we need to deploy it first. Just quite recently, google launched Google AutoML (https://cloud.google.com/vision/overview/docs#automl-vision), which enable us to build image classification or even object detection with no code at all. Today we are going to build a snack classifier, a machine learning system which able to classify what snack in the image. Select multiple images that belong to a label, then click Assign Labels, select the correct labels, and voila~.Repeat this process until we finish labelling all the images. Please bear with it because having a lot of correctly labelled image is the key to have good image classifier.Phew! When we create the dataset, there are three options that we can choose :For our case, every image has exactly one label (either cheetos/qtela/etc. When the upload process is finished, we can see all the image in the IMAGES tab.
