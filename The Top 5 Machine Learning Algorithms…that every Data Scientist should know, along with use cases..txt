Similar key concepts of decision trees can be applied to random forests — supervised, classification, and regression. It can also be much faster than a manual approach.Imagine the decision tree example from above, but apply it to each of the classes in this random forest problem. For example, a list of decisions and result could possibly be:Documentation that is easy to follow and detailed :Now we are getting into the top two Machine Learning algorithms. It is the random sample of data for the decision trees that compose a random forest. A unique benefit of using this algorithm is that it can work with not only classification, but regression problems as well. The ensemble of decision tree results ultimately make the random forest result or prediction, whichever class gets the most votes — wins. You can have, say, 20 classes or 20 types of products, but it would take hours to manually classify them, and you could make some easy mistakes.
