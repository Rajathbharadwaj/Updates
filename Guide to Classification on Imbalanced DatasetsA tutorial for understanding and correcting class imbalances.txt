For class A, where class A is the majority class, this might be equal to 0.8 (80%). We are artificially adding data points to our dataset to make the number of instances in each class balanced.How do we generate these samples? (1) Between-ClassA between-class imbalance occurs when there is an imbalance in the number of data points contained within each class. If you have an algorithm that predicts all of the positive class correctly but also has a large portion of false positives, the precision will be small. ENN removes any example whose class label differs from the class of at least two of their neighbor. Undersampling is recommended by many statistical researchers but is only good if enough data points are available on the undersampled class. SMOTE generates new samples in between existing data points based on their local density and their borders with the other class.
