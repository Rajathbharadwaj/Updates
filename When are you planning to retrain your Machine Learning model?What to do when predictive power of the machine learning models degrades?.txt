When ground truth is not available at the time of model trainingIn most of the machine learning models, the ground truth labels are not available to train the model. For example, the models such as fraud detection, cyber-security etc receive manipulated and distorted inputs which cause model to output misclassified predictions. In short, it is critical to keep your machine learning model updated; but the key is when? Or, should we keep adding the new data without removing older data. Lets figure out below:To understand this, let us recall one of the most critical assumptions in ML modelling — train and test dataset should belong to similar distribution. Such type of adversaries also drives down the model performance.3. You may find a lot of tutorials which would help you build end to end Machine Learning pipelines.
