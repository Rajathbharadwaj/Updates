So, your conditioning image like the edge image here has to exactly match the respective handbag image. The key idea here is that you have one conditional GAN that inputs x as the conditioning image and generates then some new output. Also, the discriminator then gets the generated image, but it also gets access to the conditional vector y. So, you cannot just use it with a conditional GAN.The key ingredient, here, is the so-called cycle consistency loss. This then gives rise to the following concepts: So, you take two generators and two discriminators, one GAN G is generating y from x. So let’s say if you have text to image generation then, of course, the image should depend on the text. Han Zhang, Tao Xu, Hongsheng Li, et al.
