Consider this function:The statement at the end,creates a new type containing all of the data and functions that we put into the creation of that type. Consider these first two example functions:A DataFrame is a type that cannot be iterated element-wise in the same way that we typically would with an array. There are some disadvantages, like not being able to read types or access data without writing functions to do so, but I think that trade-off is certainly worth it. This is incredibly useful for creating objects with mutable data and child methods to manipulate that data. This, of course, is a staple of Python as an example, where the __init__ function is called nearly every-time a new type is created. This can be both data; in the Int64 example of number, or it can be functions; in the example of double() or add5(). So with that in mind, we can combine the two concepts by initially creating our objects with a dispatched function, and then creating our variables with methods as children.
