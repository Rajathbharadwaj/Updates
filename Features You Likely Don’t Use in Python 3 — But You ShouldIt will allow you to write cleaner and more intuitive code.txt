Most people who use Python prefer to use the format method.In addition to the format, Python 3 provides a versatile way of interpolating strings via f-strings. It looks like the same code as above using f-strings:f-strings are incredible, but strings such as file paths have their own libraries that make it easier to manipulate them. The following example is provided by Python’s official documentation.In Python 2, every folder above had to have an __init__.py file which turned that folder into a Python package. This can be used to demonstrate, for instance: thousands, hexadecimal and binary numbers.With Python’s latest version, the walrus operator is introduced, which makes a variable assignment of expression. One of the most well-known changes is; print function in Python 2 is replaced by the print() function in Python 3. As a convenient abstraction for working with file paths, Python 3 provides pathlib. However, parentheses work in Python 2 if a space is added after print keyword because the interpreter evaluates it as an expression.
