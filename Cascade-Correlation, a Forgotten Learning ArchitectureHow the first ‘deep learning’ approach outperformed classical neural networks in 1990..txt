Until the training has finished, the hidden neuron is disconnected from the network. training the outputs and training a hidden neuron. Since we are working with Python, we need to import some basics and PyTorch:The CasCor algorithm starts off with a one-layer network (only output neurons). we will be using a single output neuron and connect that to our input (and bias). Particularly, training the neuron only converges with a very low probability to the weights that produce the displayed values, i.e. In 1990, a dynamic neural network architecture by Scott E. Fahlman and Christian Lebiere called “Cascade-Correlation” outperformed static neural networks in training speed, accuracy, and size. We’ll also see some results of applying it to a simplistic problem domain.In the next parts, we’ll investigate how to better automate and optimize the algorithm.
