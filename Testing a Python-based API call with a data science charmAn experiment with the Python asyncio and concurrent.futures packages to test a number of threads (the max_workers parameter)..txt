But then the duration (shown in seconds to complete a request) gets unstable and jumps again by 100 threads.All red dots shows runs that had not fetched any data. At first glance, no connection between duration/speed, number of threads, and retrieved data. 2 runs with 100 threads both threw the similar error:That means I exhausted either network, or server, or both. This is a good reason to take the next steps towards performance optimization, like reducing the amount of data to retrieve, or considering another source. This reduces the total execution time.The question of an optimal number of threads arose in the following regard. Then I looked over the average performance per number of threads.I had only 9 runs, so the movies column became a bit confusing. I was not controlling this parameter.Each API call sends one and the same request and downloads the same amount of data.
