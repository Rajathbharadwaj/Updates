In case the reader is on another schema version, the reader may pass into the Avro parser two different schemas. The parser then uses its resolution rules to translate data from the writer schema into the reader schema.Let’s check that out on our BackendComponent.convertAvroBinarToJSON method. In our tutorial, we use the global unique fingerprint as the primary identifier in ES, which makes the lookup straightforward (see screenshot above). An encapsulation technique used to be capable of replacing the persistency manager in a future version.Again very compact to get an Avro Schema persisted. After the initial schema is defined, applications may need to evolve, as shown in the above diagram. The 8-byte little-endian CRC-64-AVRO fingerprint of the object’s schema. Interested in JEST, check out Blaedung’s tutorial article.Our ESPersisencyManager just shields our tutorial classes from direct Jest exposures.
