Each row gets a “Rolling Close Average” equal to its “Close*” value plus the previous row’s “Close*” divided by 2 (the window). To do so, we’ll run the following code:We’re creating a new column “Rolling Close Average” which takes the moving average of the close price within a window. To do so, we run the following code:We’ve defined a window of “3”, so the first calculated value appears on the third row. In contrast, a running calculation would take continually add each row value to a running total value across the whole DataFrame. Rolling window functions specifically let you calculate new values over each row in a DataFrame. To do this, we simply write .rolling(2).mean(), where we specify a window of “2” and calculate the mean for every window along the DataFrame. Rolling calculations, as you can see int he diagram above, have a moving window.
