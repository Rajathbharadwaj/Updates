Without threading, CSV.jl is 2 times faster than R, and is about 10 times faster with 10 threads.Mortgage risk datasetNow, let’s look at a wider dataset. The dataset has 4 Million rows and 25 columns and values of types Int, String, Float, Missing.Single threaded data.table is 1.25 times faster than CSV.jl. However, with more threads, Julia is either as fast or slightly faster than R.Wide dataset: This is a considerably wider dataset with 1000 rows and 20,000 columns. At 10 threads, it is about 14 times faster than data.table.Uniform String dataset(II): The dimensions of this dataset are the same as that of the one above. The four columns with prices are float values, and there is a date column.The single threaded CSV.jl is about 1.5 times faster than R’s fread from data.table. Pandas’ read_csv takes 34s to read, this is slower than both R and Julia.Mixed dataset: This dataset has 10k rows and 200 columns. CSV.jl gets about 4 times faster with multi-threading.Summary Charts:Across all eight datasets, Julia’s CSV.jl is always faster than Pandas, even when artificially limited to a single core.
