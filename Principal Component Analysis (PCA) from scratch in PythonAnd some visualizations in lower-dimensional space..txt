The second value is an array, representing the explained variance percentage per principal component.The first two principal components account for around 96% of the variance in the data. What I mean is that principal component analysis won’t give you the top N features like for example forward selection would do. For PCA this means that we have the first principal component which explains most of the variance. Orthogonal to that is the second principal component, which explains most of the remaining variance. Principal Component Analysis is a mathematical technique used for dimensionality reduction. For fun, try to include the third principal component and plot a 3D scatter plot.And that does it for this article. To do so we’ll need to ditch the second principal component.
