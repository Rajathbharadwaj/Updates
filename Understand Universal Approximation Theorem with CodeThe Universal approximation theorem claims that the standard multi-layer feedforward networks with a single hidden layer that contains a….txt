However, we can use neural networks to approximate such complex relationships by adding more hidden layers and neurons. The experiment with 2 hidden layers can definitely approximate better if we spend more time tuning the hyperparameters. It can be observed that the approximation of the predicted function is much better without spending much time tuning the training parameters which is of expectation. I suggest if the function is too hard to approximate, go ahead and add more layers and neurons! It can be observed that the results are even better than using 2 hidden layers with bad hyperparameters. The relationship of image pixel values to the classification of the image is ridiculously complex where not even the best mathematician can possibly come out with an appropriate function. This is due to the fact that training the neural network does not always provide us with precise/perfect values.
