There is large enough variance within the dataset that enables each data point within the training data to have an independent effect on the network. Within deep learning the convolution operation acts on the filters/kernels and image data array within the convolutional layer. The values within the vector all add up to 1.Dropout: Dropout technique works by randomly reducing the number of interconnecting neurons within a neural network. The purpose of an activation function as a component of a neural network is to introduce non-linearity within the network. This function is called further down the machine learning workflow.So far, we have obtained and partitioned the dataset and created a function to process the dataset. But why do we do it?When conducting data aggregation, it is common to consecutively accumulate images or data points that correspond to the same classes and labels. Therefore we can have a network that generalizes well to the entire dataset, rather than a subsection of the dataset.2.
