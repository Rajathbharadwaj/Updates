Also when the desired query gets way more complex?For raw SQL and query builders, you have to go out of your way to come up with similar complex queries. The example query from above can be built and executed like this:Note that the resulting query is still the same as in the raw code. To make it reusable you would expose the query q somewhere.The query builder prevents typos in the offered parts — .select, .from_ , .where in the example above. After that initial effort you need to make sure that the database is in sync with the query builders code base. The correct way, of course, would be:Which results in this query:The point of this example is not that ORMs make it hard to do the right thing. On the other hand, developers are not aware of what they actually query from the database or how many queries they send. In other words: A query builder solves problem 1 and 2, addresses problem 3, and still has problem 4 and 5.ORMs create an object for each database table.
