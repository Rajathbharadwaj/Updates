Now we will apply the Random Forest model.The Random Forest model can have its hyperparameters optimized to improve the model. Finally, let’s see what the ROC curve can tell us!That is a much more beautiful curve! Its maximum value of impurity is 0.5 and maximum purity is 0.Entropy, as well as Gini, is a measurement of disorder of data. However, to have a first feeling of the model, we will apply it with its default values. We are going to use the Gaussian type that is used when the predictor has continuous values.Random Forest can be used for classification and regression tasks. Gini is the measure of the probability of incorrect labeling a randomly chosen value from the data set. But be aware that this is an essential part of any machine learning problem.There are a couple of conclusions to be made looking at the data.
