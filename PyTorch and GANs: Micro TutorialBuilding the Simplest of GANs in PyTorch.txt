Let’s start with the Generator:Add the following to your script:Our Generator class inherits from PyTorch’s nn.Module class, which is the base class for neural network modules. In our forward method, we step through the Generator’s modules and apply them to the output of the previous module, returning the final output. Let’s walk through it line-by-couple-of-lines:You know this one!Sample some real samples from the target function, get the Discriminator’s confidences that they’re real (the Discriminator wants to maximize this! Here, the backward method calculates the gradient d_loss/d_x for every parameter x in the computational graph.Apply one step of the optimizer, nudging each parameter down the gradient. This is very similar to the generator’s training step.Sample some generated samples from the generator, get the Discriminator’s confidences that they’re real (the Discriminator wants to minimize this! When you run the network (eg: prediction = network(data), the forward method is what’s called to calculate the output.Nope! Easy.Feed the latent vectors into the Generator and get the generated samples as output (under the hood, the generator.forward method is called here).
