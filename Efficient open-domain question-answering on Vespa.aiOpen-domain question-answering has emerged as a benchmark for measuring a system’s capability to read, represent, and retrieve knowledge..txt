We set up the first phase ranking profile like this:Here, we set up three ranking profiles, one for sparse retrieval, one for dense retrieval, and one for hybrid retrieval. The first stage, retrieval, is done using either term-based retrieval or embedding based retrieval. A retrieval-based system uses search terms or semantic representation vectors to recall a set of sentences or paragraphs subsequently evaluated with a machine-learned model to extract the exact answer. They are also used in a document BERT-base encoder model to create the embedding vector for dense retrieval. The Dense Passage Retrieval (DPR) paper uses ElasticSearch as the providing system for BM25.Embedding-based (dense) retrievalThe number of potential terms in a vocabulary can be vast indeed. Retrieval-based question-answering systems require connecting various systems and services, such as BM25 text search, vector similarity search, NLP model serving, tokenizers, and middleware to glue all this together. The actual model we use is a pre-trained model the DPR team has published on HuggingFace’s model repository.
