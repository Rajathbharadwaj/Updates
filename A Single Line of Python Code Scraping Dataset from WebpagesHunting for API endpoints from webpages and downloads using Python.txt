So, let’s select the XHR filter.XMLHttpRequest (XHR) is an API in the form of an object whose methods transfer data between a web browser and a web server. Extremely easy!I believe you must use Pandas Dataframe if you are a data scientist or data analyst. However, when there is an available API endpoint on the website, why not just find it and directly use it?Life is short, use Python! Let’s just take one more example, the IKEA website.Let’s say you want to get all the beds from IKEA together with the product details to perform some analysis. But, of course, this approach is not going to be effective for all the website, and that is why the web scraping libraries are still necessary. Particularly, retrieval of data from XHR for the purpose of continually modifying a loaded web page is the underlying concept of Ajax design. Let’s find its API endpoint!Most of the popular web browsers provide their developer consoles now.
