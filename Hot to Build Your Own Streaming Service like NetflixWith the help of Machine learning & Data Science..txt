The weighted average can help us achieve that.In the weighted average approach, you multiply each rating by a similarity factor(which tells how similar the users are). The mathematical formula for the average rating given by n users would look like this:What does it mean? For example, two users can be considered similar if they give the same ratings to ten movies despite there being a big difference in their age. Now, it’s time to involve Data science and Machine learning to make streaming service be personalized. The first lines of the dataset can look in the following way:This snippet of the dataset shows the precise rating a user gave to a particular movie. I guess the path of creating a streaming service can be individual for each programmer or data scientist. Using only the CDN system is commonplace for a streaming giant like Netflix, which has gone a step further than other streaming services.
