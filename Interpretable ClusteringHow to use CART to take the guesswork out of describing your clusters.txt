Specifically, we can plot the Decision Tree, using:By inspecting the Decision Tree, we can highlight the characteristics of the corresponding cluster. It works by inspecting the density of the nearby samples, assigning samples to the cluster if the density is sufficiently high. Specifically, after having clustered the unlabelled data, we can assign to each sample the corresponding cluster as a label. This would be repeated for each cluster, giving you different simpler decision trees which are far easier to understand, and later present. We can iteratively run the K-Means algorithm with an increasing number of K until a somewhat large number (e.g. It also does not perform well when the density of points is heterogeneous, meaning that in some areas of the distribution the density is higher than in others. Usually, the options are:K-Means is the most common unsupervised learning technique, mostly due to its simplicity and effectiveness.
