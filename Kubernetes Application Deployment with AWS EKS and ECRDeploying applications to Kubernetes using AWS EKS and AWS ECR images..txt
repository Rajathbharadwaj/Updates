At the get issue following command to check whether our cluster is deployed.Sometimes you may get the following error when you issue the kubectl command. I am using Node.js with express to create a very simple web application that will be listening on port 3000.Next, let’s dockerize our web application. At the end of the stack creation, it will give 3 outputs.We can create clusters easily by giving eksctl create cluster command. The next task is to push our image to AWS ECR.Before we can push the image we need to create a repository on ECR. Let’s first try to identify where are the pods of our application are running.From that, we can identify the nodes of the pods that our application is running. When we create our cluster, we need to specify the VPC subnets for our cluster to use. For that create a Dockerfile and issue docker build command.Now if you issue docker images we will see our webapp image.
