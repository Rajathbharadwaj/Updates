This indicates that the path(self) method to be used as a getter function.Similarly, @<property_name>.setter decorator helps to define the setter method. In our case, @file_name.setter has been added just before the file_name(self, value) method to define it as a setter method. They do the same job, but the latter is considered more intuitive and easy to read.We have _path and _file_name private attributes in our CSVGetInfo class.
