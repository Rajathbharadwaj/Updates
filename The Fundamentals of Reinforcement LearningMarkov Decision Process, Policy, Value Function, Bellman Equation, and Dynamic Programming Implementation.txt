Recall that we can define an optimal policy if and only if the value function of one policy is equal or bigger than other policies in any given state. At its core, dynamic programming algorithm uses Bellman equation iteratively to do two things:Policy evaluation is a step to evaluate how good a given policy is. However, in real life, we often don’t know what the optimal policy is.To find the optimal policy, the dynamic programming algorithm is normally applied. To achieve this, it means that the agent should follow a policy that maximizes the value function we have just discussed above. In order to maximize the reward, the agent needs to choose which action it needs to take in a given state such that it gets a high cumulative reward. First, under any given policy, the corresponding value function is computed. Thus, the action-value function is normally denoted as qπ.In some sense, the value function and the reward have some similarities.
