(time.sleep(x) for x in range(3)) is a generator while [time.sleep(x) for x in range(3)] is a list even though it uses range(). Every time you do next() call to the iterator object, you would get the next item in the sequence until the iterator object is exhausted and raise StopIteration. Therefore, we can simply write our function as a generator.Lazy function — generatorLazy property — decoratorAnother common use case of customized Lazy Evaluation is the initialization of class properties. His question is:Why does a lambda map object like x = map(lambda x: x*2, [1,2,3,4,5]) doesn’t take any space? In the following example, the property cities takes longer time because it needs to invoke an API to get a list of city names. As the size of the list increases, more memory is used.However in Python 3, range(5) returns a range type. When you do list(x), you basically compute all the values at one time.
