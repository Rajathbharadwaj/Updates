Suppose, we want to evaluate the following involving five Numpy arrays, each with a million random numbers (drawn from a Normal distribution),Here is the code. For using the NumExpr package, all we have to do is to wrap the same calculation under a special method evaluate in a symbolic expression. The code is in the Notebook and the final result is shown below,This is a Pandas method that evaluates a Python symbolic expression (as a string). You are welcome to evaluate this on your machine and see what improvement you got.Let’s dial it up a little and involve two arrays, shall we? Also note, how the symbolic expression in the NumExpr method understands ‘sqrt’ natively (we just write sqrt).Whoa! Numpy and Pandas are probably the two most widely used core Python libraries for data science and machine learning tasks. You can read about it here.Basically, the expression is compiled using Python compile function, variables are extracted and a parse tree structure is built.
