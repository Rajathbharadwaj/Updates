The steps are almost similar to gremlin server’s settings (since JanusGraph is built on top of gremlin server). We need to copy the graphml data to the janusgraph container (/opt/janusgraph/data/), so that it can be loaded to janusgraph later by commands issued from the client. I’ve recently been looking for an open-source, distributed graph database, as I need to store a large graph data somewhere persistently. Roughly speaking there are a few things that you need to do (this example is done for TinkerGraph in gremlin server).First, edit the gremlin-server yaml file. The code below will pull the latest janusgraph image from the docker repository.Copy a graph dataset to the docker. You can get the file from here.Create a remote.yaml file for gremlin remote connection settings. We’ll do a few things within the console.First, we’ll connect to the remote janusgraph server.
