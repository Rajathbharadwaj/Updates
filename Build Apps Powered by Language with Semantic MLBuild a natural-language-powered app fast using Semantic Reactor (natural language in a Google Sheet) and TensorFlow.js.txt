Using a sentence encoder model, she built a video game world that infers how the environment should react to player inputs using ML. Using sentence embeddings, we can figure out if two sentences are similar. I put these sentences in the first column of my Google sheet:You’ll have to use your imagination here and think of these “actions” that a potential character (e.g. In particular, the model learns which words tend to occur in the same spots in sentences. They’re learned by a machine learning model through data. This is useful, for example, if you’re building a chatbot and want to know if a question a user asked (i.e. points plotted on an n-dimensional grid) such that the distances between points are meaningful.
