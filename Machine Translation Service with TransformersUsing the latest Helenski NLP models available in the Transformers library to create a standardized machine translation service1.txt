That way, there’s one service per language pair and each pair can scale individually as demand increases. However, I’d like to share a few concerns with this approach.These translation models are pretty big (as in each one is like 300MB). Let’s do it.Huggingface has done an incredible job making SOTA (state of the art) models available in a simple Python API for copy + paste coders like myself. Now we can download the models we need with a single command. This also gives us more control over the languages we want our service to support from the get. This is the functional equivalent of “let’s wrap machine learning in a flask API and docker image” but, whatever, it’s fun. Here’s a screenshot from the output using postman.Not going to go in depth here, but the logical next step would be to take this to kubernetes for true scale.
