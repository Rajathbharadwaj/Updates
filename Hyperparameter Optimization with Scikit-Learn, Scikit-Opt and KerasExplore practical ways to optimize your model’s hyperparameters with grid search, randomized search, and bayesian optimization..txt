However, it’s very time consuming, and probably not the best use of your time.Fortunately, there are many techniques to explore a search space much more efficiently than randomly. The grid search will explore 32 combinations of RandomForestClassifier’s hyperparameter values, and it will train each model 5 times (since we are using five-fold cross-validation). Since there are many hyperparameters, it is preferable to use randomized search rather than grid search. First, when creating your search space you need to make each hyperparameter’s space a probability distribution as opposed to using lists likeGridSearchCV. First being the hyperparameters we want to experiment with and the values we want to try out. For example, we can use GridSearchCV or RandomizedSearchCV to explore the hyperparameter space. I hope this convinces you to stray away from your comfort zone using GridSearchCV and RandomizedSearchCV and try implementing something new like BayesSearchCV in your next project.
