To help me identify software vulnerabilities in my container images, I enable ECR image scanning.Here I am using the AWS Management Console to complete the creation of the function. When I select the image, the Lambda is translating that to the underlying image digest (on the right of the tag in the image below). The container image support is offered in addition to ZIP archives and we will continue to support the ZIP packaging format.There are no additional costs to use this feature. You can also use the AWS Serverless Application Model (SAM), that has been updated to add support for container images. In this way, the function is using the same image even if the latest tag is passed to a newer one, and you are protected from unintentional deployments. You can use this three-stage approach to build your own custom images:I create the entry.sh script below to use it as ENTRYPOINT. You can update the image to use in the function code.
