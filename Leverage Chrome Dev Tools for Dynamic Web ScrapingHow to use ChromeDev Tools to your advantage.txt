This is how javascript orientated websites work, they make HTTP requests to a server and the displayed data appears on the website. The temperature data could change and most likely those changes are reflected at a server level which then can be displayed without constantly updating the website. Often an API endpoint is used to serve responses that carry data the website has displayed on modern pages. HTML Form usually invokes javascript to post data to a server that will authenticate and respond with the information you want. Javascript has the ability to invoke HTTP requests and is often a source of changing the information on a page without rendering the page. We are trying to mimic the HTTP request to grab data from the website. We are in a much better position to access this data now.Now we are ready to see if we can grab the information easily.9.
