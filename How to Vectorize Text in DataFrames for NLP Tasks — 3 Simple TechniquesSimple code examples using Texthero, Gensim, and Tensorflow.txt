Using my own pipeline, I can preserve as much card context as possible for similarity:Notice I use fillna since some cards do not have text. This way it is easy to compare how the various techniques clustered similar cards together.Each technique produces different plots, but clumps similar cards together. Load the data into a pandas DataFrame.Notice I use the pandas read_sql function to generate a dataframe using raw SQL. Complete Code is towards the bottom!Instead of scraping data from one of my favorite sources, I’m going to explore cards from the game Magic: The Gathering. Doc2Vec commonly outperforms simple-averaging of Word2Vec vectors, so it is worth exploring!At the basic level, the Doc2Vec algorithm gives a document another floating word-like vector, called a doc-vector. Instead, I’ll use the simpler route: Doc2Vec.Introduced in 2014, Doc2Vec is a Model that represents Documents, paragraphs or sentences as a Vector. Hovering over a point will reveal the card name and the card text.
