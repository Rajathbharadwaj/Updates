Bubble maps do not have the inherent big area bias in choropleth maps.In this tutorial, we have seen how to create both static and interactive bubble maps, using Python. See the below GIF.Bubble maps can be an alternative map to display a numeric variable with different bubble size, instead of the most used choropleth maps. In the next section, we are going to see how you can create an interactive bubble map.There are different Python libraries for plotting interactive bubble maps. Notice that we first normalize the data (Age80_w) by subdividing the total population.Alternatively, we can use bubble maps and avoid some of the pitfalls of choropleth maps. administration areas) with values (i.e., age subdivisions), bubble maps can replace Choropleth maps. The size of the circle represents a numeric value on a Geographic area.We often use Choropleth maps to display areas, and in that case, we use a colour encoding. We have numerous columns of the population subdivision (Age 5 to age 80 and above) for each administrative unit (Deso).We usually use choropleth maps and use a colour encoding.
