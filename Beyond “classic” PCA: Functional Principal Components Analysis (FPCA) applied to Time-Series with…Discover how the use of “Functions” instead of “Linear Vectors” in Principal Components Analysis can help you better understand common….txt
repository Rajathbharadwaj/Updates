(*the temperatures are recorded at the “department” level, which is a smaller scale than regions in France (96 departments Vs 13 regions). (As you might have guessed, temperature records are — by design — well aligned since they are captured simultaneously. The illustration below provides a relatively simple example to understand this mechanism. )Functional Principal Components AnalysisNow that our dataset is “warped”, we can run a Functional Principal Components Analysis. The FDASRSF package allows horizontal, vertical, or joint analysis. You just need to check the anaconda logs in case of failure to identify them. However, we rename “Department” into “Region” for an easier understanding of readers.
