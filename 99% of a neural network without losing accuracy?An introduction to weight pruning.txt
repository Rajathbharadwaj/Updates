To fit complex architectures into production constraints, one can reduce the size of the models by pruning certain weights. This would not help with the training itself, but would reduce computational requirements for inference.The process of eliminating weights is called pruning. In addition to obtaining a minimal network, they had a more ambitious goal: the resulting architectures should perform well with random weights. This way, no training would be needed.In essence, their method is an evolutionary search in the space of possible architectures. the loss) increases significantly when a large portion of the weights are removed.One particular challenge arises with this method when the pruned network is retrained. So, restricting the search space also acts as a regularizer.However, this is not a simple task, as reducing the model’s capacity can also lead to loss in accuracy. )They proposed the following iterative pruning method.Continuing the training with pruned weights is necessary.
