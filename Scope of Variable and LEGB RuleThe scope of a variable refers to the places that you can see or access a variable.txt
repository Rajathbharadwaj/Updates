If it doesn’t find that variable, it searches the scope of the outside function, which is called an enclosing function because it encloses the inside function. Note that the global value accessed is the value at the time the function is called, not the value when the function is defined. However, any time we call the variable in the local scope of the function, it will look in the local scope first. The last one is enclosing functions, and we will discuss this later in the nested function section.Let’s check out an example here.We define the function and then call it. In this example, we define a function power_of, which contains an internal function called inside. This is called a nested function.Let’s look at another example here.The syntax for the inside function is the same as that for any other function. That’s why calling square(5) returns results in 25 and not 30.If Python cannot find the variable in the local scope, it will then look in the global scope.
