If only one category is present, the feature will be dropped entirely.‘if_binary’ : drop the first category in each feature with two categories. This parameter exists only for compatibility with sklearn.pipeline.Pipeline.Transformed input.Return feature names for output features.String names for input features if available. When this parameter is set to ‘ignore’ and an unknown category is encountered during transform, the resulting one-hot encoded columns for this feature will be all zeros. Encode categorical features as a one-hot numeric array.The input to this transformer should be an array-like of integers or strings, denoting the values taken on by categorical (discrete) features. This parameter exists only for compatibility with sklearn.pipeline.Pipeline.Fit OneHotEncoder to X, then transform X.Equivalent to fit(X).transform(X) but more convenient.The data to encode.Ignored. The features are encoded using a one-hot (aka ‘one-of-K’ or ‘dummy’) encoding scheme. See help(type(self)) for accurate signature.Fit OneHotEncoder to X.The data to determine the categories of each feature.Ignored.
