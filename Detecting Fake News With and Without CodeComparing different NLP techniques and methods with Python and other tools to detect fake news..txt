So even though the tweets were real, they contained fake information.The company tweeted:This is not the first time this happens, and probably not the last. Pretty standard in text analytics and NLP.For modeling, we have this function to plot the confusion matrix of the models:Split the data:Logistic regression:I got an accuracy of 98.76%. The bigger problem here is what we call “Fake News”. Can we stop this from happening?The problem is not only hackers, going into accounts, and sending false information. So I’ll only put the most important pieces.Important: To be able to use some of the functions you will need to select at least one column of the dataset. You can now download the final dataset with the model if you want to get the metrics in Python or somewhere else. You can even compare the models with the Compare Text Classification Models.
