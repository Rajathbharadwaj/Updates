They let you use state and other React features without writing a class…A hook is a special function that lets you “hook into” React features. The Provider component has a prop called value(line 14 in the first photo) which takes in data that the Provider wishes to pass onto descendant Consumer components. This object always comes with a Provider component (as can be noted by object destructuring in line 5 of the first photo) and Consumer component. After the update, the component re-renders because of the state update and executes the same useEffect() hook again. It’s very easy to incorporate into functional components:In the screenshot above, we can take note of a few things. (React.js Docs)useStateuseState(), as defined above, hooks state into a component. (React.js Docs)One thing to take note of is that you can call these two variables, [state, setState], whatever you want.
