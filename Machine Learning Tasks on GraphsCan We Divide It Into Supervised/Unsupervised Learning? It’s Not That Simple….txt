It means that this learning task on graphs is very similar, if not the same, to classification, regression, and clustering tasks that are used in standard machine learning problems. Similar nodes are typically closer together which is known as homophily.Because the data independence doesn’t work in this case anymore, we can’t classify this classification task as supervised learning. Also, each node has a respective feature vector xi (weight, height, and colour), where i means that the vector belongs to the node Vi. Here this assumption fails because the node labels (animal type) might be dependent on other neighbouring nodes . For example, Common Neighbours is a first-order heuristics as it requires only the direct neighbourhood of the node to compute the score (and not neighbours of the neighbours). Mathematically, we can define this animal friendship network as G = (V, Ε), where V is a node (animal), and E is edge (friendship connection). They compute certain scores from the graph itself and convert it into likelihood of the link between two nodes.
