It allows the users to change the desired logging library during deployment.At first, it may sound counterproductive to use additional facade library SLF4J for logging. While these logging libraries offer concrete implementation, SLF4J offers abstraction or facade for various logging libraries. Like Apache Commons, it is also Modular and contains many independent libraries.It covers essential utilities, collections, string manipulation, concurrency utilities, graph libraries, I/O utils, hashing, and many more. For a large project, the performance of a logging library can be crucial, especially the asynchronous logging, peak throughput, latency. If you are working on a big project and not using any Apache Commons library, then you are probably re-inventing the wheel.Google Guava is another top-grade general-purpose Java library. There are also other logging libraries like Log4j, Log4j 2, Logback, which provide advanced logging in Java. Hibernate is also modular and offers a core module and many functionalities based modules.Main features:HTTP is by far the most used and prevalent Application-Layer protocol.
