Second, the cluster name is a function if the set of points that define the cluster itself. To test the inclusion of a random point in the cluster, we would have to convert its coordinates into an H3 index and compare it to the cluster’s list. The code above also inserts the new cluster points in the database, as it progresses.We are now sure that all our clusters are connected and stored in the database. My approach was quite visual, so I browsed the whole set of cluster points, looking for a reasonable threshold value. This function calculates the K nearest locations to each of the input points and uses a simple voting mechanism to retrieve the top two most common street names. For each cluster point, we store the correspondent id (might be -1 for outliers), the geographic location, and the level 12 H3 index. So what about naming them?Follow this section in the notebook 7-cluster-names.ipynbThe final challenge in this article is to derive the cluster names without using a reverse-geocoding service.
