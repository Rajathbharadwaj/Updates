Let’s start with the simplest one.Oracle likes this step done with two views — one for training data and one for testing data. This is broken down into multiple steps, such as train/test split, model training, and model evaluation. If you’ve been studying data science, it’s likely you know how to perform machine learning tasks in languages like Python, R, and Julia. Now we can download the data and load it:When a modal window pops-up simply provide a path to the downloaded CSV and click Next a couple of times. To interpret:And that’s pretty much it for the model evaluation.And there you have it — machine learning project written from scratch in SQL. Here’s how this table looks like:It has many more rows (39 x 3), but you get the point. In-database machine learning is the answer.We’ll use Oracle Cloud for this article.
