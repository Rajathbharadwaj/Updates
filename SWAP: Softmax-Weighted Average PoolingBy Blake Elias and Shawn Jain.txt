With better gradient estimates, could we take larger steps by increasing learning rate, and therefore converge faster?Sparse gradients discard too much information. From AlexNet [cite] to ResNet [cite] to Reinforcement Learning [cite cite], it’s widely used.Many variants have been developed; Average-Pooling outputs the average, instead of the max, over the window. In practice, in the backward pass, all positions corresponding to the maximum simply copy the inbound gradients; all the non-maximum positions simply set their gradients to zero. In doing this exercise for convolution and pooling, we noted that max-pooling only computes a gradient with respect to the maximum value in a window. If its inputs (dL/dO — the outbound gradient of Max-Pool) — were dense, could dL/dF be a better estimate of the true gradient? )Forward:Backward:Note also that dL/dF is not sparse, as each sparse entry of dL/dO sends a gradient value back to all entries dL/dF.But this raises a question. The forward pass is best explained as pseudo-code:average_pool(O, weights=softmax_per_window(O))The softmax operator normalizes the values into a probability distribution, however, it heavily favors large values.
