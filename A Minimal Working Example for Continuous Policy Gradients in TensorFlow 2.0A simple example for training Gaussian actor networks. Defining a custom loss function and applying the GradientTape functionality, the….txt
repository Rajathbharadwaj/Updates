Let’s worry about that later though and first present our custom Guassian loss function:So we have the correct loss function now, but we cannot apply it!? First, in our custom loss function we make a forward pass through the actor network — which is memorized — and calculate the loss. For neural networks, it may not be as straightforward how we should perform this update though.Neural networks are trained by minimizing a loss function. Indeed, we will need to define a ‘pseudo loss function’ that helps us update the network . These steps yield the following loss function:Quite similar to the update rule, right? The actor network learns and outputs these parameters.Let’s formalize this actor network a bit more. As the loss is only the input for the backpropagation procedure, we first drop the learning rate α and gradient ∇_θ.
