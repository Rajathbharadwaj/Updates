The query results are stored as a new table within our dataset called o3monthlymax.This query ran in 37 seconds and processed 627.8 mb of data. GCP offers a suite of cloud technologies with fully managed and serverless solutions that make processing, storing, and analyzing data easy. We join this data with county geometries from the 2016 US census to make the data viewable in Geo Viz. Create a new notebook instance in the AI platform, the one used here is customized to use n1-standard-1 machine type which helps minimize costs. This demo uses a fraction of these credits.BigQuery is a serverless data warehouse and analysis tool with a familiar SQL interface. For example, Nebraska and Kansas have very few ozone monitors.Next we will perform air pollution prediction using Prophet. Notebook instances on the AI Platform come already authenticated to access BigQuery in the same project.Once the VM is provisioned you can open jupyterlab directly from the GCP console.
