Since it’s a composite action we’ll use the using: "composite" syntax:If an action is defined in the same repository as the workflow we can refer to it using ./path-to-action-file. Since we want to run a script from our own repository we also need to use the checkout action to access it. Steps can run commands, run setup tasks, or run an action. Since it’s defined in a public repository we just need to pass it’s name:The with syntax is a map of input parameters defined by the action. Let’s use the example from GitHub Docs and create an action that prints a “Hey [user]” message. To do that we’ll use an action: setup-python.After that, we can run the commands as we usually do in a machine. In other words:Let’s first create a workflow with no action just to understand how it works.
