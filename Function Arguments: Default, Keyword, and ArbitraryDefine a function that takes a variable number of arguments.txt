In the function definition, we use the parameter *args, then turns all the arguments passed to a function call into a tuple in the function body. Notice that this function calculates the first argument to the power of the second argument, and the default second argument value is 1. This is called a flexible argument.In this example, we write the function that sums up all the arguments passed to it. In the function header, we attach the parameter of interest with an equals sign and the default argument value. Say that you want to write a function but aren’t sure how many arguments a user will want to pass. Then, in the function body, we need to print all the key-value pairs stored in the dictionary kwargs.For your reference, we can use args and kwargs in combination. To write such a function, we use the parameter kwargs preceded by a **.
