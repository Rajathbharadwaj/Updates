We will cluster each zipcode based on their housing characteristics using several popular clustering algorithms.In the first step, let’s import our data set to Tableau. Using TabPy, Tableau can run Python script on the fly and display the results as a Visualization. I use a Zipcode to create a Map to visualize the clustering results. This dataset has 13 columns.As our primary goal is to see how we use TabPy, We will not focus on making the best possible model. So we need to change the Default Table Calculation to Zipcode to make this code works.Now, it’s time to visualize the results. We can use the parameter to change the number of clusters.Let’s celebrate coming up to this point! If you don’t, you can install it first by going to https://www.python.org/ to download the python installation.
