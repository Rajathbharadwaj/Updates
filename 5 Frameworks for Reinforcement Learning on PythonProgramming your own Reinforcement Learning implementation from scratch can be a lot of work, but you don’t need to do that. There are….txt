I have made a list here of some frameworks I have come to know and use along time, with their benefits and cons. When I say “simplest to understand code” I refer not to use, but to customize it and utilize it as a building block for your project*. As such, it has been developed for building readable, efficient, research-oriented RL algorithms and contains implementations of several state-of-the-art agents such as D4PG, DQN, R2D2, R2D3 and more. The Keras-RL github also contains some examples that you can use to get started in no time. It was conceived so researchers could compare their RL algorithms easily, using as a baseline the state-of-the-art implementations from OpenAI — thus the name. It is even possible to train an agent for OpenAI Gym environments with a one liner:Acme comes from DeepMind, probably the most well-known company working on RL in research. The problem is that there is no standard yet, and so finding support online for starting, fixing a problem or customizing a solution is not easily found.
