For other students, the expression was not NULL so it returned the expression itself.The COALESCE function returns the first non-null value in a given list. Unlike the ISNULL function, it can accept multiple expressions.Syntax: COALESCE(expression[1…..n])For example, SELECT COALESCE(NULL, NULL, 'red', 'blue', NULL) returns ‘red’ as it is the first non-null value. The former is short and simple, but the latter is more clear and easy to understand.The NULLIF function takes two expressions and returns NULL if the expressions are equal. For Stan, Email2 is returned as the Primary_Email as it is the first non-NULL value in the COALESCE function. One of my friends, who recently started learning SQL, asked me about NULL values and how to best deal with them. Of course, we did not cover each and every edge case here, but this should be a good starting point for a beginner. In fact, COALESCE is like a shorthand forCASE.
