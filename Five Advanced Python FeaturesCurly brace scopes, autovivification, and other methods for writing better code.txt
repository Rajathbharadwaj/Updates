[Out]: [10, 12, 14, 16, 18]The syntax we are using here uses the same start, end, step pattern — slice(start, end, step). Here, we assign a slice to a variable, like so:We can then apply this named slice to any list. [Out]: [6, 10, 14, 18]The following advanced list slicing method is called named slices. Python will always find something to surprise us with — there are simply far too many brilliant features crammed into the language. Take a lookCreate a free Medium account to get The Daily Pick in your inbox.Written byWritten by Make learning your daily ritual.
