To know the shape of the data set use shape.It means that the data set has 768 rows and 9 columns. Let’s check it.To get the column name in a list.Transpose the data frame.To display the particular range of rows in the data frame. So having a zero value does not make any sense also.The Glucose level variable also has some zero values. The zero value in the Skin Thickness variable is very much.BMI and Insulin variables also containing zero values in it. So, that we can do more data wrangling.We want to know the exact numbers of the diabetic and non-diabetic person in the outcome variable. We must do data cleaning and treatment with outliers for data visualization. From the data set, we observe that two variables are in the float and remaining are integer values.
