One simple solution is to swap the x and y axis mappings:In our next example, we will use two bar plots to get different insights for each categorical variable. In this section, we will look at bivariate histograms to inspect relationship between two numerical variables.Let’s plot both of them for all combinations of numerical column pairs. The charts on the right will also work if you have a numerical target.Histograms and kernel density plots are great ways to check out the distribution of numerical columns. In bar plots, one axis tells us the categories while the other tells us the numerical value associated with that category. We will treat species as the target for the rest of this post except for the second half of the next section on bar plots. Here’s an example of what I mean:The area under the curve is proportional to the frequency of species in the first plot because common_norm=True normalises density among everyone. When working with certain types of machine learning algorithms, looking at feature importance of the model gives us insights on what features the model deems more important.
