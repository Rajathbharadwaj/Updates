Of course, the larger number of datasets combination generated from autoPP module, the more time needs for further processes in OptimalFlow automated machine learning. More package’s information could be found at PyPI.After data preparation in Part 1 of this tutorial, most of the features are ready to feed the modeling process. Even in the autoCV (OptimalFlow’s model selection & evaluation module), you could custom set specific models or hyperparameters searching space. Missing values in category features are not welcomed when the data flow comes to autoPP(OptimalFlow’s auto feature preprocessing module). You could read another story of its introduction: “An Omni-ensemble Automated Machine Learning — OptimalFlow”.Set up your working environment with Python 3.7+, and install OptimalFlow by Pip command. So we need to double-check the cleaned data and apply data cleaning to the features with missing values. In the previous Part 1 of this tutorial, we discussed how to implement data engineering to prepare suitable datasets, feeding further modeling steps.
