For that we need to set up:We’ll use the move_by_offset method of the action object to move the cursor from its last known position to the next. If the pace is too small, the cursor we’ll take a long time to move to the next date in the chart. This happens even though selenium keeps that last position, but you can also notice the cursor keeps going further left in each new loop at the pace we established. An infinity loop will make the cursor move until the limit date we set up and. Each time the cursor moves to the left, it scrapes a new value and date from the label. With that done, just use send_keys method to write the search and the click method to click on the search button. If it is too big it might skip dates.However, it’s inevitable that the sometimes cursor will still be on the same date even though it just moved.
