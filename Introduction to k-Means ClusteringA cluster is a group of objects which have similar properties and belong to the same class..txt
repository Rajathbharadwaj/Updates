Now we would update the centroid by considering the newly added point in the clustersOutput:K-means clustering is widely used technique for data cluster analysis. In clustering, we first partition the set of data into groups based on the similarity and then assign the labels to those groups. It is either less than or equal to the number of data points in the data set. We start with each object considering as a separate cluster and keeps on merging the objects that are close to one another. Also, there is an additional condition that says each object can belong to only one group.The partitioning method starts by creating an initial random partitioning. Clustering is an unsupervised learning technique which is used to make clusters of objects i.e. It is down until each object in one cluster or the termination condition holds.
