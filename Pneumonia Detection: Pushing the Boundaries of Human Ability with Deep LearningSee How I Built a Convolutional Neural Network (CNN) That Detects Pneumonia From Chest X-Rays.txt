diversity, to avoid the model from overfitting to training data.Deep learning is really an art of craft, and you really could construct your model however you’d like. This brings up the question on whether our model can accurately detect Pneumonia if patients do not reside in China or are in a different age group? Let’s see the code in action.Our model seems to have high recall and AUC score, but there could still be blindspots due to the limitations of dataset. While common ratios for split used are: 70% train, 15% validation, 15% test, my validation set is 0.27% of the entire data. However, the validation set only contained 16 images, and each class has 8 images. Data augmentation is a good practice to not only to add more data to the existing dataset but also add minor alterations, a.k.a. We do that by first having our model optimize for AUC score, focusing on having a good measure of separability for our binary classification.
