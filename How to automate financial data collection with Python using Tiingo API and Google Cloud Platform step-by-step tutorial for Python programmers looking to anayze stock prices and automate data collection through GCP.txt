Import the SP500 members table yousaved as a csv file in Step 1; this can be easily done with the .read_csv method in pandas.4. It then defines the source path (uri) in which to find the new data to upload, and loads it with the .load_table_from_uri method and relevant parameters. Scrape data: You then need to fetch the relevant url content with the request.get method. Once done, all you need to do is declare it in the code and set it as a variable, such as the illustrative example below.3. Define API Token: to interact with the API, you need to get an API Key, or token, in order to identify ourselves as registered Tiingo API users. The uri points the the GoogleCloudStorage location of the data. Component_headers is nothing but a sublist of df_columns, which indentifies the columns pertaining to the first of the two tables with the ID of constituents.4.
