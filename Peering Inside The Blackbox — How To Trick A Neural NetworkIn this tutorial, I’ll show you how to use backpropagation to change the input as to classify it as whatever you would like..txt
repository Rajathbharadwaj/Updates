Unfortunately, with certain types of inputs (images, sound, video, etc…) explainability certainly becomes much harder but not impossible.How would a neural network answer the same questions I posed above? In a random forest, we can look at decision nodes.In neural networks, it comes down to how creative we are at using gradients. If we add this with the input we get the following result:In this case, I’m not particularly convinced that we’ve turned this 5 into an 8. First, the black areas are where the network things we need to remove pixel density from. If I show you the following input:there’s a good chance you have no idea whether this is a 5 or a 6. Neural networks get a bad reputation for being black boxes. Second, the yellow areas are where it thinks we need to add more pixel density.
