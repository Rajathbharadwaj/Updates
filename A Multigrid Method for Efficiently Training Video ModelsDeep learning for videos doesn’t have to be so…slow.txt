Because this paper uses the multigrid method for the training phase and not the testing phase, we end up having different shapes for the training and test data. According to Wu et al (2020),“[t]he span is the support size of the grid and defines the duration or area that the grid covers. A Multigrid Method for Efficiently Training Video Models. We increase the sub-mini-batch size whenever we increase the mini-batch size due to short cycle changes. :)We still haven’t defined the schedule for adjusting the sampling grid (for both time and space)! With short cycles, we cycle through smaller and larger spatial grids, but all within a small range. The authors found that adjusting according to the short cycle caused worse results in practice, so they avoided this.Fine-TuningOkay, you’re probably confused by why I’m suddenly bringing up fine-tuning.
