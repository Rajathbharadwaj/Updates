Of course, we have the different weights g subscript j, the bias g subscript 0, and the error that we introduced by approximating g(x). So, you see that if we do errors in u(x), they get potentially amplified by g(x). If we now relate this to classical pattern recognition, then we could interpret u(x) as a feature extractor and g(x) as a classifier. So, this is the approximation using G(x) and U(x). We can see we can also use the definition of G(x) that is a linear combination of sigmoid functions. So, this is nice, but we kind of get stuck here because the error of the approximation of u(x) is inside of the sigmoid function. Now, if you’re approximate it, we introduce some error e subscript g. The error has to be added back.
