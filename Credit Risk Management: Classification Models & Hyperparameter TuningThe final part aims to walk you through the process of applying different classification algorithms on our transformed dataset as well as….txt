Possible reasons that might explain the large gap is (1) overfitting the train set, (2) leaking target to the test set. Basically, this technique splits the training set into n folds (default = 5), then fits the data on n-1 folds and score on the other fold. However, after cross checking, it doesn’t seem as the case.Hence, I decided to look into another scoring metric, Cross Validation Score, to see if there’s any difference. Yet, it’s the otherwise for the test set when the scores are remarkably low (over 50%). This process is repeated in n folds from which the average score will be calculated. That’s the wrap for this end-to-end project with regards to Classification! In this case, XGBoost and LightGBM can be considered as the successor of Decision and Random Forest.
