We first tested the correctness of our implementation by calling model.eval() and running the model through the MLM forward functions. In AllenNLP, you can keep track of all the vocabularies you need in an AllenNLP Vocabulary object using namespaces such as the label namespace and input namespace. and simply making sure that the dynamically generated mask_idx is shifted forward by 2 before being fed into the model. Since the model, in this case RoBERTa-large, has been pretrained with MLM, we would expect it to do very well on MLM. It consists of an input-noising objective, where given a text, the model has to predict 15% of the tokens given the context. That was not the case, and we were getting very high losses.It became clear why: the predictions were always 2 off from the gold labels. Special thanks to Phu Mon Htut for editing this post.If you’d like to see the final implementation, check it out here!Written byWritten by
