Probably the original trend was not linear to begin with.Let’s compute the RMSE and the R²-score between the data and the trend we’ve extracted. Let’s subtract the trend from the data and see how the detrended data looks like:Not very convincing. But let’s see what the numbers say about it:Compared to the linear trend, with the polynomial trend R² has increased and RMSE has decreased. The parameters of the quadratic expression are found using linear regression.What does the trend look like?Much closer fit, isn’t it? We will come back to these numbers later.If the trend is not linear, we could try to fit it with a polynomial curve. Let’s assume it’s linear — let’s do linear regression and find out. But here’s the thing: even if the curve we’re fitting is a high degree polynomial, it’s still linear regression that will be used to find it.
