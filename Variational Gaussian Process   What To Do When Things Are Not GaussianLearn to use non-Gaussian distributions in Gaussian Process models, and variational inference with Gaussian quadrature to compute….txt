At the same time, we want q(f) to stay close to our prior, through a small KL distance between q(f) and p(f). What should q(f) look like?We want q(f) to look like the posterior p(f|y), but we don’t know the structure of the posterior. The random variable f is integrated out.We want the shape of q(f; μ, Σ) to be as close to the shape of p(f|y; l, σ²) as possible. This equation confirms that maximizing the ELBO is equivalent to minimizing the KL divergence between q(f) and p(f|y) because a larger ELBO means a smaller KL(q(f)||p(f|y)). Similarly, you can introduce other non-Gaussian distribution, such as student-t to handle outliers.After we described the prior p(f) and the likelihood p(y|f), it’s time for the posterior p(f|y). Our model uses the correlation k(X_*, X) matrix to link the random variables f at training locations X and the random variables f_* at testing locations X_* together. If for some f, q(f) and p(f|y) evaluate to the same probability, then their ratio is 1, and log(1) is 0.
