For example, node 1 will have a node feature of 1, node 2 will have a node feature of 2, and so on. The fact that node 3 has a higher degree than node 4 and 5 leads to a lower weighting of node 3’s features in DAX. For example, the first row of AX corresponds to the sum of nodes features connected to node 0, which is node 1, 2, and 3. The graph G will consist of 6 nodes and the feature of each node will correspond to that particular node number. But why would node 3 have different values after normalization if it has the same initial value as node 4 and 5?Let’s take a look back at our graph. The insertion of A in the forward pass equation enables the model to learn the feature representations based on nodes connectivity. To simplify, we are not going to assign edge features in this experiment.Output:Since we only have 1 graph, this data configuration is an example of a Single Mode representation.
