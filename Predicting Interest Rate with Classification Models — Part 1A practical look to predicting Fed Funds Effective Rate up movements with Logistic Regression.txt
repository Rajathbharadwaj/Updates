It is a type of Generalized Linear Model that predicts the occurrence’s probability of a binary or categorical variable utilizing a logit function. Unfortunately, it is not the purpose of the post, so I am going to make a basic solution and transform them into the average value of my variables. Recall tells us how many of the true positives our model capture through classifying them as positives. We know that accuracy itself is not that informative, so let's look at the classification report and the ROC curve.The classification report gives us Precision, Recall, and F1-Score. The attempt to solve the problem using Logistic Regression turned out to give us an accuracy of 66%, predicting 810 labels correctly. So we are going to stick to them and fire up our model!So there it is! To deal with this issue we can use some methods for oversampling data.
