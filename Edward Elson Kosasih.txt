There are two reasons why.During my literature review, I stumbled upon an information-theoretic framework to analyse the link prediction problem (Tan et al. …I’ve recently been looking for an open-source, distributed graph database, as I need to store a large graph data somewhere persistently. What we didn’t realise back then is that even if one knows of all variables’ values, a slightly inaccurate approximation could result in a totally different future. If you’re one of them, we’d just like to say a huge thanks for paving the way for us future committees! One standard way to solve this problem is to perhaps perform linear regression i.e. We first introduce the model and provide a code implementation to run this for a small graph. I haven’t evaluated all existing graph databases, so I can’t say much about how JanusGraph fares against the other solutions.
