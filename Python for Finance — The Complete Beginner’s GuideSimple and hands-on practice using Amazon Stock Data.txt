After running this method, we can also see that our data is sorted by the date index.Another helpful method we will call is the tail method. For example, let’s say we want to see the AMZN stock data where the closing price is more than 3000 and the volume is more than 5 million. We will convert the rest columns into a numeric value, for this exercise we can go with float numeric type.Great, we solved the data type issue. Let’s check the data types of our columns.As you can see above, Volume column is the only integer type and the rest is an object type. In default, the describe method will return the summary statistics of all numerical columns, for example in our example all columns are numerical. On this website, you can find stock data for different companies and practice your skills using those datasets. It helps us to see more than just rows of values, it gives us a better picture of the data.
