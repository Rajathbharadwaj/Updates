If someone talks about deep neural networks, one means neural networks with many hidden layers. 436–444 J. Schmidhuber, Deep Learning in Neural Networks: An Overview (2014), arXiv: 1404.7828 I. Goodfellow, Y. Bengio, and A. Courville, Deep Learning (2016), MIT Press, ch. Generally, the number of hidden layers is known as the depth of the network, whereas the number of circles in one layer stands for the width of the network. In my opinion, this is the most complex point in neural networks, since the used optimization algorithms are usually very sophisticated. To name just two frequently used algorithms: Stochastic Gradient Descent and Adam optimizer.All of these optimization algorithms seek for adapting the weights such that the loss function is minimized. The blue arrows are called weights and they are the parameters of the network.The input values and the output values are connected by hidden layers. So, each of these hidden layers can be understood by basic knowledge in linear algebra.Assume now we walk once through the neural network.
