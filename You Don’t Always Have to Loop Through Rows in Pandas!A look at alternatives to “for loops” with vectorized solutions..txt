This solution only took 40 milliseconds to run.Now let’s create a new column called “super_category”. We’ll import the data and create two DataFrames, one called “old” and one called “new”. This way, we can create some broad categories to label each user review as “bad”, “ok”, and “good”. Here, we’ll identify if people qualify as a “super reviewer”, or in this case, if the length of their review is greater than 1000 characters. Yes, we could have done everything with loops, and you can even see that the same structure applied across many different operations. (To understand the logic behind the for loop codes below, please check out my previous piece on it, as it already has an in-depth explanation on the subject. )Let’s create a new column called “qualitative_rating”.
