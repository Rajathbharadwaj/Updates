Finally, we train the model using the .fit(...) method.To evaluate our model we use the test set of the cats and dogs dataset consisting of 2'000 images. to install our dependencies we can run the following code within a notebook cell:In this tutorial, we work with a dataset consisting of cats and dogs images. In this tutorial, we will go through a Google Colab Notebook to train an image classification model using only 5 labeled samples per class. It might very well be that there are more similar images of cats in our dataset than images of dogs. If you only label a few samples out of your dataset one of the key questions arising is how do you pick the samples? To achieve this we can create an embedding of our dataset followed by a sampling method called coreset sampling. If we want widespread adoption of ML we need to find ways to train them efficiently, with little data and code.
