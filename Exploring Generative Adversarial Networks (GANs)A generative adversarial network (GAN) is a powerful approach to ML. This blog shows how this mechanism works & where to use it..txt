In this sense, the ability for the generator to improve its output, somewhat competes with the discriminator’s ability to classify data as training progresses. From an implementation standpoint, the generator and discriminator have their own loss functions, where the loss function of the discriminator contains the loss function of the generator. The GAN for such a problem would look as follows:The generator is seeded with random noise (data) and generates an image of a handwritten digit. This output is then fed to the discriminator along with images from the MNIST dataset (the training data). Since data generated by GANS can be indistinguishable from real-world data, you don’t have to ponder much to realize the potential implications of this. It starts with random input1, and repeatedly generates data that approaches the quality of real-world data. And when put to good use, the generative and classification components of a GAN can potentially help ML practitioners build solutions which perform feats that seem like magic.1.
