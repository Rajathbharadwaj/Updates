The first and most common is F1, which combines precision and recall measures to find the optimal confidence threshold where precision and recall produce the highest F1 value. )It would be nice if we could directly quantify how each model does across images in our test set, across classes, and at different confidence thresholds. In order to do this automatically, we need to train an object detection model to recognize each one of those objects and classify them correctly. For example, in medical images, we might want to be able to count the number of red blood cells (RBC), white blood cells (WBC), and platelets in the bloodstream. The computer vision community has converged on the metric mAP to compare the performance of object detection systems. By the way, did you know there are cow sharks?As the model is getting less confident, the curve is sloping downwards. )The models both predict bounding boxes which surround the cells in the picture.
