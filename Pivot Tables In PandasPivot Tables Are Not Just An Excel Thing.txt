So let’s make a pivot table where we group by age_bin along the row axis, and gender and passenger class along the column axis. That is:Now let’s code up our pivot table (we round the output to 2 decimal places to make it easier to interpret):And here is pivot2. So the next time you analyze your data, throw up a few pivot tables first. But today we’re just here to demonstrate how to build and use pivot tables with Pandas so we will be more cavalier about dropping data. If you’re a frequent Excel user, then you’ve had to make a pivot table or 10 in your day. So given that it includes many of the best parts of Excel, it’s only natural that Pandas has its own implementation of Excel’s famous pivot table. Note that the bins are ranges, but I named each bin using just the greater bin edge.
