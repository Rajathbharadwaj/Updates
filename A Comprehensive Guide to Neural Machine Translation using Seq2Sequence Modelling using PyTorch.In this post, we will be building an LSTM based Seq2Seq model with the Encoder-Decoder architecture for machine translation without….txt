We can access the source (german) batch of data using the .src attribute and it's corresponding (English) batch of data using the .trg attribute. Word Vector: Convert a word from a higher dimension to a lower dimension (Word Embedding).7. Also, we can see the data before tokenizing it.I just experimented with a batch size of 32 and a sample batch is shown below. The Encoder of the Seq2Seq model takes one input at a time. BucketIterator :Here our source language (SRC — Input) is German and the target language (TRG — Output) is English. The sentences are tokenized into a list of words and indexed according to the vocabulary. Words to Integer Mapper: Map words into integer numbers for the entire corpus and vice versa.6.
