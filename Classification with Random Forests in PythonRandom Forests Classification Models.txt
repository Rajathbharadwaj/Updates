In the case of classification, the output of a random forest model is the mode of the predicted classes across the decision trees. The random forests algorithm is a machine learning method that can be used for supervised learning tasks such as classification and regression. The algorithm works by constructing a set of decision trees trained on random subsets of features. We showed how to transform categorical feature values into machine readable categorical values. Let’s also initialize the “KFold” object with two splits. We will us ‘KFold’ to validate our model. Additionally, we will use the f1-score as our accuracy metric, which is the harmonic mean of the precision and recall.
