An important characteristic of Lasso Regression is that it tends to completely eliminate the weights of least important features (i.e., set them to zero). One of the major problem in Linear Regression algorithm is it tries to over-fit the training set and when accuracy is checked on test set it performs verrryy bad! Here in Ridge Regression, one more term is added to Cost function i.e Regularization term.‘θ’ denotes weight(slope) applied to the parameters in datasets. Therefore, to penalize weight(slope), Regularization methods are used.Ridge Regression Equation is given by,We have seen the first part of this equation i.e. without any Regularization), Ridge or Lasso?It is always advisable to use some amount of Regularization so as to eliminate over-fitting in case of Linear Regression. On the other hand, if we put lower value of α, model is prone to over-fitting. The hyper-parameter α controls how much you want to regularize the model.
