We can see in previous outputs that some values are NaN which means “Not a Number”, and we have to deal with these missing values to get good results. Remember that True meaning 1 and False meaning 0, so to get all the True values returned by isnull() function, we can use sum() function. It also tells us about memory usage which is 83.6 KB in this case.describe()Describe is also a super useful function to analyze the data. In that case, it will automatically start index from 0.Here we can see that the index starts from 0 and goes on till the Series end. Remember square root is any number raised to power 1/2.Pandas DataFrameDataframe is indeed the most commonly used and important data structure of Pandas. We are collecting all the rows from the dataframe where df[“Sex”] == “male” and df[“Survived”]==1. Let’s take an example where we only want to see the entries where “Sex” is “male”.
