Polynomial regression is one of the most fundamental concepts used in data analysis and prediction. prefixed functions are the tensorflow symbolic counterparts of the functions known from numpy.In this article, we have revisited the polynomial regression problem in both mathematical and programmatic terms. Assuming y’s dependent on x is expressed in the following form:we speak of polynomial regression (with ε denoting a noise term). If we choose to be the degree, the hypothesis will take the following form:Next, we choose the metric by which we express the cost function. It can be any parametrized mathematical formula, however, curve_fit imposes one condition: the model-function itself accepts data x as its first argument. Naturally, if the maximum n = 1, the problem becomes linear regression.To solve the problem means to determine the values of all aₙ’s to represent the data well. The Polynomial class defines a callable object based on the polynomial expression (the first equation).
