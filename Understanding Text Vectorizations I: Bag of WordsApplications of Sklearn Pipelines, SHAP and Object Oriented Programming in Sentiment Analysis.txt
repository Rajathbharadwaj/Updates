For example, a review contains more positive words like “great”, “fantastic” would be considered more positive than a review with words like “so-so”. Since reviews all have different lengths, we will need to pad each review with 0s for the words that are not contained in the review. We would like to find the fraction of specific words that are strong indicators of positive or negative sentiment. With the help of model explanation package SHAP, we can produce a visualization of feature importance on a single review (more about SHAP in a different post). We will start by loading in the reviews from our data source.The data we will use for this project are the reviews from John Hopkins University’s multi-domain sentiment dataset. Let’s have a look next.At this point, we would often raise the question of how to choose between the two best performing model (LightGBM and Multinomial naive Bayes). In other words, all we are doing with state-of-art deep learning models are simply trying to squeeze more performance improvement out these classification problems.
